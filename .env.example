# Server (copy to server/.env for local dev; root dev script runs from each workspace)
SESSION_SECRET=change-me-in-dev-only-min-32-chars-please-000
DATABASE_URL="file:./prisma/dev.db"
PORT=3001

# Seed (optional override; see server/prisma/seed.ts for defaults)
# SEED_USER_PASSWORD=password123!
# SEED_ADMIN_PASSWORD=password123!
